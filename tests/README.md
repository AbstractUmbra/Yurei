# Yurei tests

The `*.es3` files in [the input directory](./inputs/) are not real file types, instead these are partial deserialized payloads I've come across and need to be able to read, parse and (de)serialize correctly.
